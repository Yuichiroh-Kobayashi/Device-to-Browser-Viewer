@AGENTS.md

## Claude Code adapter

- Treat `AGENTS.md` as the primary repository guidance for this repository.
- Keep this file a thin Claude-specific adapter; do not duplicate the project
  manual here.
- Before broad or multi-file changes, produce a short plan.
- Destructive Git operations and any remote mutation (push to `main`, force
  push, merge, tag/release changes, GitHub Issue/PR mutation) require the
  user's explicit authorization for that exact action.
- Unless the user specifies otherwise, give questions, progress updates, and
  final reports in Japanese.
